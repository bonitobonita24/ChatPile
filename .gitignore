# Secrets & credentials
.env
.env.*
!.env.example
.auth/
tampermonkey-script.md
Makefile.local

# Docker compose (dev only — use .example as template)
docker-compose.dev.yml

# Node dependencies
node_modules/

# Build output
dist/
build/

# IDE / session history
.specstory/
.claude/
